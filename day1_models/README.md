# Day 1: Models + PromptTemplate

## 目标
学会用 LangChain 调用 LLM，理解 PromptTemplate 的拼装方式

## 任务清单
- [ ] 跑通 ChatOpenAI 基础调用（兼容 MiniMax API）
- [ ] 学会用 `PromptTemplate` 拼装 system / user / assistant 消息
- [ ] 理解 `ChatMessage` 和 `HumanMessage` / `AIMessage` / `SystemMessage` 的区别
- [ ] 完成练习：用 MiniMax API 调一次"你好，请介绍自己"

## 验收标准
能独立用 Python 调通 MiniMax API，PromptTemplate 能动态注入变量
